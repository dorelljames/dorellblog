import posed from "react-pose"

export const Section = posed.section({
  // enter: { staggerChildren: 50 },
  // exit: { staggerChildren: 20, staggerDirection: -1 },
})

export const H1 = posed.h1({
  // enter: { y: 0, opacity: 1 },
  // exit: { y: 50, opacity: 0 },
})

export const H2 = posed.h2({
  // enter: { y: 0, opacity: 1 },
  // exit: { y: 50, opacity: 0 },
})

export const P = posed.p({
  // enter: { y: 0, opacity: 1 },
  // exit: { y: 50, opacity: 0 },
})
